import { error } from './error';
import { info } from './info';
import { getError } from './getError';
import { getCallStack } from './getCallStack';

export const logger = {
  error,
  info,
  getError,
  getCallStack,
};
